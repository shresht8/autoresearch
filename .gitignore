# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results file
results.tsv

# Per-run artifacts (untracked so they survive branch checkouts)
run.log
experiments/

# Agent memory (filesystem-level, must persist across checkouts — not branch-specific)
.claude/agent-memory/
claude.zip
gh200-parallel-training-guide.docx
